import React from 'react';

const AvailableEnrollment = ({ date }) => {
    return (
        <div>
            <h2>this is available enrollment {date.toString()}</h2>
        </div>
    );
};

export default AvailableEnrollment;